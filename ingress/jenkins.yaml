apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "jenkins-ingress"
spec:
  rules:
  - http:
      paths:
      - path: /jenkins
        pathType: Prefix
        backend:
          service:
            name: my-jenkins
            port:
              number: 8080
