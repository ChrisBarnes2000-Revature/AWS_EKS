controller:
  # aws address # add you address here. Then uncomment
  jenkinsUrl: http://addbb1a6df1d644109f685feb12730f5-1924653928.us-east-1.elb.amazonaws.com
  # Do not change anything below
  # ingress. tell jenkins your using ingress routes.
  jenkinsUriPrefix: /jenkins
  ingress:
    enabled: true
    apiVersion: "extensions/v1"
    # ingress controller watches for this path on URI.
    path: /jenkins
    annotations:
      ingressClassName: nginx
      # kubernetes.io/ingress.class: nginx
  additionalPlugins:
  - github:1.34.5
  agent:
    namespace: jenkins